# =============================================================================
# Perfil LOCAL — desenvolvimento. Não use em produção.
# =============================================================================

server.address=localhost
server.port=7000

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
app.swagger.abrir-navegador=true

jwt.secret=RamosSomarJwtSecretKey2026ConvertixSegurancaMuitoForteParaAssinaturaHS256

app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:7000,http://127.0.0.1:7000

# Client credentials opcionais em local (facilita Swagger)
app.client.enabled=false

app.admin.auto-create=true
